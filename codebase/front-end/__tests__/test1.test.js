//import * as oper from "../localStorage/userOperations";
//const rq = require ('pouchDB');

/*
test('creating a user', () => {
     operation.createUser('email', 'pawd', (response)=>{
          console.log(response);
     });
});*/
/*
describe("testing pushToHistory ", ()=>  {
      
     test('1 + 1 = 2',()=>{
          expect(2).toBe(2);
     });
 
 
//createMonthlyLog(parent, content, days, trackers, date, callback)
     /*
    oper.createUser('123', 'adsf', (e)=>{
          var user1 = e;
      });

     test('adding weird page', ()=>{



          let startDate1 = new Date(2021,5,24);
          let end1 = new Date(2021,5,25);
          months1 = ['May'];
          content1 = ['abc'];
          trackers1 =['abc1'];
          let futureLog;
          createFutureLog(startDate1, endDate1, months1, content1, trackers1,  (err, response)=>{
               if(err){
                    console.log(err);
               } else {
              futureLog = response;
               }
          });
          expect(expected.content).toBe(futureLog.content);
      });
      */
   //});